## Section placeholder

Replace this slide with real content.

---

### Example: Java code block

```java
public class Hello {
    public static void main(String[] args) {
        System.out.println("Hello, JDWP!");
    }
}
```

--

### Vertical slide

Use `--` (alone on a line) to start a vertical slide,
and `---` to start a new horizontal slide.

Note:
Speaker notes appear after `Note:` and are shown in the speaker view (press `S`).
