{
  "mcpServers": {
    "jdwp-inspector": {
      "command": "java",
      "args": ["--add-modules", "jdk.jdi", "-jar", "${CLAUDE_PLUGIN_ROOT}/jdwp-mcp-server/target/mcp-jdwp-java.jar"],
      "env": {
        "MCP_TIMEOUT": "30000",
        "MCP_TOOL_TIMEOUT": "120000"
      }
    }
  }
}
